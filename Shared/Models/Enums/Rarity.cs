namespace Shared.Models.Enums;

/// <summary>
/// The base rarity tier for an Echo or item, which indicates its cost and base power.
/// </summary>
public enum Rarity : byte
{
    Common = 1,
    Uncommon = 2,
    Rare = 3,
    Epic = 4,
    Legendary = 5
}
